#!/bin/bash
# DWL screenshot for Wayland with clipboard support

case "$1" in
    full)
        grim - | tee ~/Pictures/screenshot-$(date +%Y%m%d-%H%M%S).png | kapc copy -t image/png
        notify-send "Screenshot" "Full screen saved to history"
        ;;
    area)
        grim -g "$(slurp)" - | tee ~/Pictures/screenshot-$(date +%Y%m%d-%H%M%S).png | kapc copy -t image/png
        notify-send "Screenshot" "Area saved to history"
        ;;
    window)
        grim -g "$(slurp -d)" - | tee ~/Pictures/screenshot-$(date +%Y%m%d-%H%M%S).png | kapc copy -t image/png
        notify-send "Screenshot" "Window saved to history"
        ;;
    *)
        echo "Usage: $0 {full|area|window}"
        exit 1
        ;;
esac
