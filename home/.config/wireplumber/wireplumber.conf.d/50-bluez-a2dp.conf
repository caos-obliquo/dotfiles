# Keep QCY H3 Pro on A2DP (AAC) — never auto-drop to HFP voice mode.
# HFP is 16kHz mono; autoswitch-to-headset-profile was causing the "bad quality".
wireplumber.settings = {
  bluetooth.autoswitch-to-headset-profile = false
  bluetooth.profile-preference = "quality"
}
