# btop

Resource monitor (htop successor). Dracula-friendly.

## Files

- `btop.conf` — theme, layout

## Notes

- `theme_background = true`; switch to a transparent-terminal theme for the
  Dracula vibe.
- Keys: `?` shows the full keymap in-app.
