# caveman

opencode caveman-mode default.

## Files

- `config.json` — default compression level

## Notes

- `"defaultMode": "ultra"` — maximum token compression for agent output.
- The skill stack is at `~/.config/opencode/skills/caveman*`.
