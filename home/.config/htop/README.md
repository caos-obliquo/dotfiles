# htop

System monitor. Config generated by htop itself (`htoprc` is auto-rewritten
when you change settings in the UI).

## Files

- `htoprc` — column layout, hide kernel threads, tree mode

## Notes

- Custom column set: CPU%, MEM%, process tree fields.
- Run inside foot's transparency for the full Dracula look.
