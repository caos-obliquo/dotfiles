# docs

Cross-cutting documentation for the DraculaWL dotfiles.

| Doc | What it covers |
|---|---|
| [`AUTORICE-DEPLOY.md`](AUTORICE-DEPLOY.md) | One-shot deploy from fresh Arch to running desktop (`autorice-deploy.sh`). |
| [`KEYBINDS.md`](KEYBINDS.md) | Master keybind reference across dwl, tmux, nvim, zsh, foot. |

Per-component docs live next to their config: see `home/.config/dwl/docs/`
(patch index, keybinds, freeze bug writeup) and the README inside each
`home/.config/<tool>/` folder.
